spring:
  config:
    activate:
      on-profile: prd
  datasource:
    url: 실서버
    username: example_user
    password: example_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    open-in-view: false